{
  "name": "serveless-nodejs-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "serverless offline --stage dev",
    "deploy": "serverless deploy --stage prod --region us-east-2 --key $SERVERLESS_ACCESS_KEY",
    "remove": "serverless remove --stage prod --region us-east-2",
    "generate": "drizzle-kit generate",
    "migrate": "tsx src/cli/migrator.js"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.864.0",
    "@neondatabase/serverless": "^1.0.1",
    "drizzle-orm": "^0.44.4",
    "express": "^4.19.2",
    "serverless-http": "^3.2.0",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "dotenv": "^17.2.1",
    "dotenv-cli": "^10.0.0",
    "drizzle-kit": "^0.31.4",
    "serverless-offline": "^14.4.0",
    "tsx": "^4.20.3"
  }
}
